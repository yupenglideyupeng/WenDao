<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wendao.system.mapper.NewsModelConfigMapper">

    <resultMap type="com.wendao.system.domain.NewsModelConfig" id="NewsModelConfigResult">
        <id     property="id"               column="id"                />
        <result property="name"             column="name"              />
        <result property="provider"         column="provider"          />
        <result property="apiUrl"           column="api_url"           />
        <result property="apiKey"           column="api_key"           />
        <result property="modelName"        column="model_name"        />
        <result property="priority"         column="priority"          />
        <result property="maxTokens"        column="max_tokens"        />
        <result property="temperature"      column="temperature"       />
        <result property="supportJsonMode"  column="support_json_mode" />
        <result property="supportStream"    column="support_stream"    />
        <result property="usageType"        column="usage_type"        />
        <result property="timeoutMs"        column="timeout_ms"        />
        <result property="retryCount"       column="retry_count"       />
        <result property="isActive"         column="is_active"         />
        <result property="createBy"         column="create_by"         />
        <result property="createTime"       column="create_time"       />
        <result property="updateBy"         column="update_by"         />
        <result property="updateTime"       column="update_time"       />
        <result property="remark"           column="remark"            />
    </resultMap>

    <sql id="selectVo">
        SELECT id, name, provider, api_url, api_key, model_name, priority,
               max_tokens, temperature, support_json_mode, support_stream, usage_type,
               timeout_ms, retry_count, is_active, create_by, create_time,
               update_by, update_time, remark
        FROM news_model_config
    </sql>

    <select id="selectList" parameterType="com.wendao.system.domain.NewsModelConfig" resultMap="NewsModelConfigResult">
        <include refid="selectVo"/>
        <where>
            <if test="name != null and name != ''">AND name LIKE CONCAT('%', #{name}, '%')</if>
            <if test="provider != null and provider != ''">AND provider = #{provider}</if>
            <if test="modelName != null and modelName != ''">AND model_name LIKE CONCAT('%', #{modelName}, '%')</if>
            <if test="isActive != null">AND is_active = #{isActive}</if>
            <if test="usageType != null and usageType != ''">AND FIND_IN_SET(#{usageType}, REPLACE(usage_type, ',', ',')) > 0</if>
        </where>
        ORDER BY priority ASC, id ASC
    </select>

    <select id="selectById" parameterType="Long" resultMap="NewsModelConfigResult">
        <include refid="selectVo"/> WHERE id = #{id}
    </select>

    <select id="selectByUsageType" parameterType="String" resultMap="NewsModelConfigResult">
        <include refid="selectVo"/>
        WHERE is_active = 1
          AND (usage_type = 'ALL' OR FIND_IN_SET(#{usageType}, REPLACE(usage_type, ',', ',')) > 0)
        ORDER BY priority ASC
    </select>

    <insert id="insert" parameterType="com.wendao.system.domain.NewsModelConfig" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO news_model_config (
            name, provider, api_url, api_key, model_name, priority,
            max_tokens, temperature, support_json_mode, support_stream, usage_type,
            timeout_ms, retry_count, is_active, create_by, create_time, update_by, update_time, remark
        ) VALUES (
            #{name}, #{provider}, #{apiUrl}, #{apiKey}, #{modelName}, #{priority},
            #{maxTokens}, #{temperature}, #{supportJsonMode}, #{supportStream}, #{usageType},
            #{timeoutMs}, #{retryCount}, #{isActive}, #{createBy}, NOW(), #{updateBy}, NOW(), #{remark}
        )
    </insert>

    <update id="update" parameterType="com.wendao.system.domain.NewsModelConfig">
        UPDATE news_model_config
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="provider != null">provider = #{provider},</if>
            <if test="apiUrl != null">api_url = #{apiUrl},</if>
            <if test="apiKey != null">api_key = #{apiKey},</if>
            <if test="modelName != null">model_name = #{modelName},</if>
            <if test="priority != null">priority = #{priority},</if>
            <if test="maxTokens != null">max_tokens = #{maxTokens},</if>
            <if test="temperature != null">temperature = #{temperature},</if>
            <if test="supportJsonMode != null">support_json_mode = #{supportJsonMode},</if>
            <if test="supportStream != null">support_stream = #{supportStream},</if>
            <if test="usageType != null">usage_type = #{usageType},</if>
            <if test="timeoutMs != null">timeout_ms = #{timeoutMs},</if>
            <if test="retryCount != null">retry_count = #{retryCount},</if>
            <if test="isActive != null">is_active = #{isActive},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteByIds" parameterType="Long">
        DELETE FROM news_model_config WHERE id IN
        <foreach collection="array" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
